{% extends 'auth.html.twig' %}

{% block title %}Regras{% endblock %}

{% block body %}
<div class="container mx-auto px-4 py-6">
    <div class="mb-6">
        <a href="/users/create" type="button"
                class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">
            Novo Usuário
        </a>
    </div>

    <div class="space-y-6">
        <div class="divide-y divide-gray-200 overflow-hidden rounded-lg border border-gray-200 bg-white shadow-sm dark:divide-gray-700 dark:border-gray-700 dark:bg-gray-800">
            <!-- Cabeçalho da tabela -->
            <div class="bg-gray-50 dark:bg-gray-700 p-4">
                <div class="grid grid-cols-4 gap-1">
                    <div class="text-sm font-medium text-gray-700 dark:text-gray-200">Nome</div>
                    <div class="text-sm font-medium text-gray-700 dark:text-gray-200">Telefone</div>
                    <div class="text-sm font-medium text-gray-700 dark:text-gray-200">Permissao</div>
                    <div class="text-sm font-medium text-gray-700 dark:text-gray-200">#</div>
                </div>
            </div>

            <!-- Conteúdo da tabela -->
            {% if users is empty %}
                <div class="p-6 text-center text-gray-500 dark:text-gray-400">
                    Nenhum usuário cadastrado.
                </div>
            {% else %}
            
                {% for user in users %}
                    <div class="flex flex-row items-center gap-8 p-6 sm:items-start lg:items-center">
                        <div class="text-sm text-gray-900 dark:text-gray-100 flex-col w-full">
                            {{ user.name }}
                        </div>
                        <div class="text-sm text-gray-900 dark:text-gray-100 flex-col w-full">
                            {{ user.phone }}
                        </div>
                        <div class="text-sm text-gray-900 dark:text-gray-100 flex-col w-full">
                            {{ 'ROLE_ADMIN' in user.roles ? 'Administrador' : 'Cliente' }}
                        </div>
                        <div class="text-sm text-gray-900 dark:text-gray-100 flex-col w-full">
                            <a class="font-medium text-blue-600 dark:text-blue-500 hover:underline" href="{{ path('app_user.update', { 'id': user.id }) }}">
                                Editar
                            </a>
                        </div>
                    </div>
                {% endfor %}
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}